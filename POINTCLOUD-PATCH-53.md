# POINTCLOUD-PATCH-53 (v1022) — Фикс потокового режима + лассо на весь связный объект

## 1. «Потоковый с диска» ломал всё (размытие, квадраты/круги, нет удаления)
Причина: для облаков >64 млн точек кнопка строила octree и переключалась
в режим LOD-потока: редактируемое облако очищалось (_setBase([])) → удаление
переставало работать; размер точки считался от полной плотности, а рисовались
редкие LOD-узлы → размытие; и принудительно uRound=1 → круги поверх квадратов.

Исправление:
- Порог стриминга 64 млн → 130 млн. Ваши облака (78/102 млн) теперь
  грузятся ОДНИМ полным буфером — чётко, со всеми точками и с рабочим удалением.
  (RTX 5070 12 ГБ спокойно держит ~2.4 ГБ на 102 млн точек.)
- octree-режим (только >130 млн): точки теперь в том же стиле (квадрат/круг),
  что и весь вьюер, и крупнее (x1.8) — меньше размытия.
- Выключение стриминга по-прежнему перечитывает облако (редактирование возвращается).

## 2. Лассо — весь связный объект (то, что обещал)
Режим 0 «Только объект» теперь берёт объект на всю глубину связного тела:
макс. глубина захвата увеличена (шаг*60→1.2м → шаг*400→8.0м), но рез по ПЕРВОМУ
разрыву глубины остаётся — стена/пол за пустотой не затрагиваются.

## Проверки
- node --check (webgl-viewer.js, app.js) — OK; npm test — 128/128; версия v1022.

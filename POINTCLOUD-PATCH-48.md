# POINTCLOUD-PATCH-48 (v1017) — Единая кнопка «🧹 Чистка»

## Проблема
Пользователь не находил инструмент ручного лассо: он был спрятан за крошечной
иконкой ✏ (vtEdit) в правой панели без подписи.

## Решение
Все инструменты чистки собраны в ОДНУ заметную зелёную кнопку «🧹 Чистка ▾»
(renderer/index.html, id=vtTools) в правой панели. По клику открывается меню:
  1. 🖊 Ручное лассо — удалить лишнее (вкл/выкл режим лассо «насквозь»)
  2. 🧹 Авто-очистка: шум + мусор (эскалирующая, повторно = сильнее)
  3. 💾 Сохранить облако (.ply)
  4. ↩ Отменить (Ctrl+Z)
Старые отдельные иконки ✏ (vtEdit) и 🧹 (vtClean) скрыты (display:none), но их
логика переиспользуется: пункты меню программно кликают эти кнопки.

## Файлы
- renderer/index.html: +кнопка #vtTools; #vtEdit и #vtClean → display:none.
- renderer/app.js: обработчик #vtTools строит меню (в стиле geomMenu),
  открывается слева от кнопки, закрывается по клику вне; текст/подписи задаются
  в JS; пункт лассо — переключатель с индикацией активного режима.

## Проверки
- node --check app.js — OK; npm test — 128/128.
- Версия ассетов ?v=1017 (26), баннер v1017.

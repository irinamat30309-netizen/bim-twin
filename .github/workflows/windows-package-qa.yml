name: Windows package QA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-package:
    runs-on: windows-2022
    steps:
      - name: Workflow temporarily paused while syncing source
        run: echo "Source sync in progress; full QA workflow will be restored in the next commit."

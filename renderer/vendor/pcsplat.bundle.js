/* PlayCanvas splat bundle not built yet. Run `npm install` + `npm start` (or
   `node scripts/build-pcsplat.mjs`) on a machine with internet to generate the
   real bundle. Until then the app uses the built-in SplatViewer. */

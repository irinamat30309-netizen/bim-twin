# Allow optional peer-dependency mismatches (e.g. superdoc prefers pdfjs-dist v5,
# while our built-in PDF renderer stays on pdfjs-dist v4). This lets `npm install`
# succeed without forcing an incompatible upgrade of our own PDF viewer.
legacy-peer-deps=true

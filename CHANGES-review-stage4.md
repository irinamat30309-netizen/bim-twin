# BIM TWIN — изменения review Stage 4

Stage 4 расширен до bounded/two-pass out-of-core пути для поддерживаемых **ASCII/binary PLY point clouds, uncompressed LAS point formats 0–10 и ASCII/interleaved-binary/LZF-compressed PCD**: disk scan → canonical store → disk-partitioned octree. Это всё ещё ограниченный профиль, не завершение Stage 4.

## Out-of-core ветка scalar-property PLY

- `las-node.js` распознаёт ASCII и binary little-/big-endian scalar-property PLY point clouds. Два прохода по исходному файлу анализируют XYZ/bounds/axis и записывают canonical records (float32 viewer XYZ + uint8 RGB + optional normalized intensity/classification); ASCII читает bounded lines с CRLF/смешанными разделителями и без обязательного final newline.
- Передаётся source transform активного preview. Все индексированные точки остаются зарегистрированными с уже открытой сценой даже если sampled preview и полный source имеют немного разные bounds.
- `octree-build-core.js` не материализует полный `pos[]/col[]` набор: строит node representatives в ограниченном рабочем буфере, пространственно записывает остальные точки в временные partition-файлы и рекурсивно потребляет их. Финальный store содержит `nodes.bin` + `index.json`; scratch удаляется после сборки.
- `main.js` распознаёт поддерживаемые PLY/LAS/PCD профили до worker, берёт authoritative header/point count/layout вместо доверия renderer-счётчику и использует отдельные RAM/disk preflight модели. Worker сверяет fingerprint источника с открытым fd; изменённый после preflight файл отклоняется до conversion.
- Не поддерживаются как out-of-core PLY mesh, list-valued properties, vertex element не первым и layouts вне scalar profile; compressed LAS/LAZ и другие форматы пока не получают этот claim.
- Формат узла v2 хранит XYZ + RGB8 и, если поле присутствует в поддерживаемом исходнике, нормализованную intensity float32 и classification uint8; индекс/декодер остаются обратно-совместимыми с v1 XYZ/RGB. GPU viewer переключает intensity (серый) и classification (детерминированная палитра), если эти поля доступны; произвольные extra dimensions не сохраняются.
- Перед выдачей узла main-process валидирует индекс: версию/layout/stride, конечные границы, безопасные счётчики, непрерывные byte offsets, точную сумму точек, уникальный граф parent-child и совпадение итогового размера `nodes.bin`. Повреждённый индекс отклоняется до чтения произвольного диапазона. При смене потока поколение увеличивается, поэтому поздние IPC-ответы старого облака выбрасываются.
- UI обновляет общий текст прогресса для LAS/PLY/PCD scan/conversion/disk partition и сообщает ограничения.

## Uncompressed LAS out-of-core branch

- Добавлен bounded двухпроходный `prepareLasOctreeFile`; indexer поддерживает uncompressed LAS PDRF 0–10 с проверкой минимальной длины record, version/PDRF совместимости, positive finite scales, point-data bounds и LAS 1.4 extended point count. Прореживание детерминировано, глобальная точность сохраняется через double offset до преобразования viewer XYZ в Float32.
- RGB16/8 по sample depth преобразуется в RGB8 canonical node format; если RGB отсутствует/все нулевые, остаётся высотная рампа. CRS WKT читается из ограниченных VLR/EVLR, source transform/CRS, class/intensity presence и omissions фиксируются в `index.sourceMeta`.
- Compressed/unknown flags, усечённые LAS и stale source fingerprint отвергаются до commit. `parseLASFile` тоже больше не интерпретирует compressed LAS как обычные records и не индексирует молча заявленный, но отсутствующий tail.
- `octree-build-core.js` обобщён для канонических LAS/PLY/PCD-records, `main.js` учитывает optional intensity/classification в disk preflight, использует source header count и проверяет целостность индекса перед чтением; UI title/progress сообщает LAS/PLY/PCD support.

## ASCII/binary/LZF PCD out-of-core branch

- Новый `pcd-out-of-core.js` проверяет header fields/types/counts, authoritative `POINTS` и соответствие `WIDTH × HEIGHT`, binary/LZF byte extent, bounded ASCII строки, metadata comments и стабильность файла по identity/layout fingerprint. Поддерживаются `DATA ascii`, interleaved `DATA binary` и PCL field-major `DATA binary_compressed`: LZF literal/back-reference stream декодируется в ограниченных чанках во временный planar store на диске, после чего нужные поля читаются bounded chunks.
- Тот же bounded LZF decoder теперь используется обычным sampled preview parser: целый decompressed payload не держится в RAM и прежний cap 512 MiB удалён. Preview заранее проверяет свободное место temp-volume, когда `statfs` поддержан, передаёт видимый byte progress, до выделения конечных typed arrays проверяет RAM-бюджет выборки (эвристика: 128 bytes/point + 128 MiB, не более 70% доступной RAM), читает только выбранные записи и удаляет личный scratch после успеха и ошибок. Async Worker владеет уникальным temp root; parent ждёт termination перед удалением при cancel, так что cleanup не зависит от worker `finally`. Для LOD scratch удаляется после успеха/ошибки в worker output scope, а main disk preflight прибавляет полный uncompressed planar size. Preview остаётся sampled in-memory view; это не full out-of-core viewer. Reader сохраняет source transform, packed/separate RGB и CRS/units metadata, считает нечисловые XYZ, сообщает пропуск intensity/classification; модуль включён в `build.files`.
- `local-only Chromium harness (not included)` прошёл видимой LOD-кнопкой на `binary_compressed` PCD с 300 000 points / 10 209 608 bytes: preview 150 000, out-of-core index 300 000 / 9 nodes, transform совпадает, UI показывает фазу `PCD LZF → временный диск`, выключение восстановило preview и очистило disk store, PAGEERROR/IPCERR/NOHANDLER — 0. Повторный прогон дополнительно подтвердил видимое сообщение `Распаковываю PCD LZF во временное дисковое хранилище`, удаление parser temp root, 0 leaked octree directories и 0 UI/IPC errors. ASCII regression проходит payload >8 MiB с CRLF и без последней newline; interleaved binary проверен Worker/unit.
- Отдельный Chromium UI/IPC cancel сценарий использовал synthetic `binary_compressed` PCD на 12 000 000 точек (compressed file 1 636 641 bytes; raw 137 MiB). Нажатие видимой кнопки «Отмена» на фазе LZF (14%, 0/137 МиБ) отправило ровно один cancel IPC, UI показал «Импорт отменён», сцена не изменилась, Worker завершился, приватный scratch удалён, PAGEERROR/IPCERR/NOHANDLER — 0. Evidence: `QA-artifacts/stage3-4-continuation/pcd-lzf-ui-cancel-e2e.json`.

## Regression/acceptance

- `test/octree-out-of-core-ply-stage4.test.js`: 12 тестов; `test/octree-out-of-core-las-stage4.test.js`: 4 теста; `test/octree-out-of-core-pcd-stage4.test.js`: 7 тестов по packed RGB binary, ASCII/Y-up chunk boundary, sampling/invalid points, LZF literal/back-reference streaming >8 MiB, disk-space preflight, malformed/truncated/stale отказам и cleanup. `test/cloud-parse-worker-stage4.test.js`: 6 тестов, включая PCD LZF progress/cancel/cleanup и отказ RAM preflight до allocation.
- Out-of-core memory/disk budget проверки включают отдельный LZF scratch allowance и sampled preview RAM estimate (`test/octree-resource-budget-stage4.test.js`: 10/10). Повторный полный `node --test` и focused LAS/PLY/PCD/build/budget/PTX stream suite отражены в `QA-RETEST-stage4.md`; все существующие skip относятся к LAZ без optional decoder.
- Крупный внешний PLY проверялся локально на bounded ingest, source-transform parity и disk-node cleanup; исходные данные, точные метаданные и локальные измерения не включены в публичный change set и не считаются повторяемым benchmark.
- Headless Chromium/SwiftShader UI E2E: LOD budget 1M выбрал 1 055 059 points/9 nodes; GPU cache 24 012 888 bytes; source transform совпал; preview восстановлен и store удалён; PAGEERROR/IPCERR/NOHANDLER — 0.
- Fake-Electron refusal tests with intentionally under-reported `expectedPoints` rejected the request before worker/store creation in separate RAM/disk insufficiency scenarios; external source files and resource telemetry are omitted. Cancellation during `parse-convert` cleaned partial canonical/output scratch.
- Дополнительные regression checks проверяют v2 round-trip для всех комбинаций RGB/intensity/classification, legacy v1 decoding, отказ на повреждённых индексах, late-response rejection при смене облака, bounded exponential backoff при node-read ошибках, GPU upload intensity/classification и draw в обоих режимах. Ошибка подгрузки получает UI-сообщение, а повторный запрос не крутится каждый кадр. Headless Chromium/WebGL2 SwiftShader smoke подтвердил shader link, видимый streamed node, одно чтение узла, attribute-buffer upload, повторную отрисовку и отсутствие GL errors; это не замена целевому GPU/Windows UI.
- `npm run check`, `npm run test:store` и syntax-check JavaScript source files вне `node_modules`, `vendor` и `dist` прошли; точное число указано в `QA-RETEST-stage4.md`.
- Исторический hosted Windows CI сначала выявил `EPERM` при `fsync` read-only backup handle; следующий прогон также нашёл read-only handles в octree/export, CRLF-sensitive hash assertion и POSIX-only SIGKILL test. Исправлено: app-owned temp/backup files переводятся в private writable mode и открываются `r+`, integrity test нормализует CRLF, Windows crash regression завершает дочерний процесс на частичной записи. Предыдущий source commit `76173ad` прошёл GitHub Actions run `36181982890` (Linux `test`, Windows `windows-test` и build check). Эти hosted результаты относятся к предыдущей версии, не включают текущие локальные изменения v2/WebGL. Последний локальный полный повтор: 852 tests / 845 passed / 0 failed / 7 skips, syntax 232 файлов, `npm run check` и `npm run test:store` passed. Packaged Windows/ASAR acceptance всё ещё не заявляется.

Подробные логи и caveats: `QA-RETEST-stage4.md`.

## Дополнительная ASCII PLY E2E-проверка

Synthetic ASCII PLY UI/IPC test: 130 000 точек / 4 761 330 bytes, 585 nodes, `ascii-ply-two-pass`, source transform совпадает, viewer cache 130 000 точек, cleanup успешен, PAGEERROR/IPCERR/NOHANDLER — 0. Проверка включала file-input import, `bimAPI.buildOctree`, чтение node, активацию stream viewer и удаление индекса. Отдельный внешний LAS fixture UI/IPC test прошёл `las-two-pass`, восстановил preview и удалил store; источник и точные метаданные не включены. Для небольшого набора disk build вызывался напрямую через IPC, так как точки уже помещались в preview. PCD 300k прошёл именно видимой LOD-кнопкой. Крупный внешний binary PLY отдельно прошёл LOD-кнопкой в local-only harness; исходный файл и evidence не включены.

## Незакрыто

PCD LZF preview и LOD освобождены от contiguous decompression buffer; sampled preview arrays всё ещё in-memory, а RAM/disk preflights являются предварительными эвристиками, не hard quotas. Streamed viewer сохраняет/показывает RGB и optional intensity/classification, но произвольные LAS extra dimensions не индексируются, а full-cloud filtering, editing, sections, analytics и export ещё не работают поверх всего disk-backed source. Индекс временный: persistent reuse/resume/recovery отсутствуют. PLY mesh, compressed LAS/LAZ и другие форматы вне перечисленных PLY/LAS/PCD остаются memory-bound. Worker/API cancellation и видимое нажатие UI-кнопки cancel во время LZF проверены E2E; целевой Windows packaged/ASAR/GPU стенд, stress/pressure runs и benchmark 100 GB отсутствуют. Resource budgets — эвристические checks, не гарантии стабильности на всех машинах. Следовательно Stage 4 остаётся **частичным и не принятым**.

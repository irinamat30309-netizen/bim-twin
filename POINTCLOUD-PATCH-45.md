# POINTCLOUD-PATCH-45 (v1014) — Надёжная ручная чистка

## Запрос
«Хочу вручную чистить — готовый, надёжный, удобный вариант; текущий никуда не годится».

## Главное: исправлен недопатч v1013
В v1013 файл `pointcloud-edit.js` ФАКТИЧЕСКИ НЕ был пропатчен (patch11 не применён):
- `cleanVoxelDensity` не существовал → кнопка 🧹 молча падала на Python (удаляло ~29 892 из 52 млн — почти ничего).
- `_rebuild` не возвращал `removedPos` → компактный undo v1013 ничего не сохранял → Ctrl+Z не работал.

### Исправлено (pointcloud-edit.js)
- `_rebuild`: типизированная Uint8-маска + 2 прохода; возвращает `removedPos/removedCol` → undo работает.
- Добавлен `cleanVoxelDensity` + экспорт в API → встроенная чистка 🧹 теперь реально работает (без Python).

## Ручная чистка — переделана по образцу CloudCompare (надёжно)
Старый инструмент имел 11 запутанных кнопок и по умолчанию использовал ненадёжный GPU-pick («видимые точки» + «латание»).
- Убраны: ползунок «Латание», «Снять», «Накопление», 3-стадийный «Видимые».
- Остались: Лассо/Рамка · Глубина (Насквозь/Только спереди) · 🗑 Удалить · ✂ Оставить · Инвертировать · Очистить · ↩ Отменить · 💾 Сохранить.
- Дефолт при входе в Правку: **лассо + насквозь** — детерминированный CPU-выбор (проекция всех точек + тест многоугольника), без хрупкого GPU-pick. Именно так чистят про-инструменты: развернул камеру, обвёл мусор, удалил.

### Стабильность (из v1013, сохранено)
- Компактный undo (только удалённые точки) — нет OOM.
- Подсветка выделения ограничена 250 000 маркеров — нет «квадратиков» и фриза.

## Проверки
- node --check: pointcloud-edit.js, webgl-viewer.js, app.js, splatviewer.js, main.js — OK
- npm test: 126/126
